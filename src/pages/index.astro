---
import BaseIndex from "@/layouts/pages/Index.astro";

export const prerender = false;

const lang = Astro.cookies.get("lang")?.value ?? Astro.preferredLocale ?? "en";

Astro.cookies.set("lang", lang);
---

<BaseIndex lang={lang} />
